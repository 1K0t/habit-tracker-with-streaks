{
  "name": "@habit/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "format": "prettier --write .",
    "lint": "echo \"Add lint rules if needed\""
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
